apiVersion: v1
kind: Service
metadata:
  name: product-service
  labels:
    app: product-service
spec:
  type: ClusterIP
  ports:
  - port: 5556
    targetPort: 5556
    protocol: TCP
    name: http
  selector:
    app: product-service
